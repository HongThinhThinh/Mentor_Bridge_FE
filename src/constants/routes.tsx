// User Routes
export enum USER_ROUTES {
  HOME = "home",
  LOGIN = "login",
}

// Admin Routes
export enum ADMIN_ROUTES {
  ADMIN = "admin",
  TOPIC = "topic",
  SEMESTER = "semester",
  USER = "user",
  CONFIG = "config",
}

// Mentor Dashboard Routes
export enum DASHBOARD_ROUTES {
  DASHBOARD = "dashboard",
  MENTOR_PAGE = "home",
  SCHEDULE = "schedule",
}
